# Project Review & Test Verification Report

## สรุปโดยย่อ

- บทบาท: Reviewer
- ผลลัพธ์: พบว่าไม่มีชุดทดสอบใน repository และไม่มีสคริปต์ `test` ใน package.json

## การตรวจสอบไฟล์สำคัญ

- package.json: มีสคริปต์ `dev`, `build`, `start`, `lint` แต่ไม่มี `test`.
- README.md: ระบุวิธีใช้ (npm) และเตือนว่า `NEXT_PUBLIC_*` จะถูกฝังใน bundle — ถูกต้อง

## ปัญหาที่ต้องแก้ไข (แนะนำ)

1. ไม่มีชุดทดสอบ (unit/integration/e2e)
   - ข้อเสนอ: เพิ่มอย่างน้อย unit tests ด้วย Vitest หรือ Jest + RTL
   - เพิ่มสคริปต์ใน package.json: "test" และ "test:watch"
2. เพิ่ม CI (GitHub Actions) ที่รัน: install, lint, test, build
3. เพิ่ม pre-commit hook (husky) เพื่อรัน lint และ tests
4. ระบุใน README ขั้นตอนการรัน tests และข้อกำหนดเครื่องมือ (node/bun)

## วิธีตรวจสอบที่แนะนำ (คำสั่ง)

- ติดตั้ง deps: `npm install` หรือ `bun install`
- รัน lint: `npm run lint`
- (หลังเพิ่ม) รัน tests: `npm run test`

## Next steps (ถ้าต้องการให้ช่วยต่อ)

- ช่วยสร้าง skeleton ของ unit tests สำหรับ component สำคัญ (Builder)
- ตั้งค่า GitHub Actions CI ที่รัน lint+test (Builder)

---

Report generated by: AI assistant using Copilot CLI runtime in VS Code
Date: 2026-05-19
